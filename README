color ascii image pipe for headless display
similar to https://github.com/mitchaiet/ASCIITerm
but different
TODO 
  proper CLEARING
  imagein imageout options (right now the output from the program is on the buffer size set in the sh, the python size is the desired out for both size set)
  some way to handle kibbie piping
reqs

xvfb
xwd
ffmpeg
python-pillow

to run:
./asciiterm.sh <program to display>
